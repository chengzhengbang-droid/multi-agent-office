# 事件日志与投影

**适用范围**：`src/core/event-store.ts`、各 `*-read-model.ts` 与 `apply*Event` ——
append-only 日志、从日志算出来的读模型、以及旧日志的回放兼容性。

这一层是整个项目的事实源，所以关于它的两条判断已经升进规矩，改这一层之前先读：

- [规矩 4 · 影响判定的状态，必须能从事件日志重建](rules.md#4-影响判定的状态必须能从事件日志重建)
- [规矩 5 · 旧日志按它写下时的语义回放，不按今天的语义](rules.md#5-旧日志按它写下时的语义回放不按今天的语义)

另有一条相邻的教训在 [frontend.md](frontend.md)：显示位置不对时不要去改投影的挂载点，
"这条状态属于谁"和"它该画在哪一行"是两个问题。

## 变更日志

新的写在最上面。

（这一卷还没有独立条目——目前的判断都已经进了规矩 4 和规矩 5。）
