# 成长记录

这个文件记的是**判断**，不是改动。改了什么 `git log` 里有，抄到这里没有任何价值。
这里只记：哪个假设错了、哪个坑是踩过才知道的、下次遇到同类问题该怎么想。

## 怎么写

**每次改完代码，在「变更日志」最上面加一条。** 但先过这一关：

> 下一个 Agent 如果不知道这条，会不会重犯同样的错？

会 → 写。不会 → 这次就没有教训，明确写「无」，别凑数。
跟着已有模式加一个字段、改一句文案、补一个测试，多数时候确实没有教训——
一个塞满流水账的文件等于没有文件，因为没人会读第二遍。

其余几条规矩：

- **一条记录 = 一个能迁移到下次的判断**，不是一个 changelog 条目。写"背景 / 教训 / 以后怎么做"，
  重点在后两项；背景一两句能让人看懂就够。
- **被审核者挑出来的问题，尤其值得记**。那是别人替你发现的盲区，是这个文件最贵的原料。
- **发现旧条目被推翻了，就地改掉并注明**，不要留两条互相矛盾的记录让后来者自己猜。
- **同一类教训出现两次以上，提炼进「沉淀下来的规矩」**。变更日志会越来越长没人看完，
  规矩那一节是这个文件真正被读的部分，所以它必须保持短。

## 沉淀下来的规矩

按被验证的先后排，不按重要性。每条都必须能指回一次真实的改动。

### 1. 上限不是判据

资源上限（轮数、重试次数、超时）回答的是"还能不能继续"，不是"该不该继续"。
一旦让"额度用完"和"真的出问题了"共用同一个出口，绝大多数事件都会是前者，
而日志会把它们全部记成后者——于是这个信号就废了，读日志的人还以为自己在看真问题。

**做法**：每条升级/失败路径都要能说出自己的**原因**，不同原因走不同分支、留不同的
escalation 值。上限只做兜底，并且要在文档里明说它是兜底。

出处：`04fd933` —— 审核轮数用完被当成"双方谈不拢"，导致几乎每个任务都要人工介入。

### 2. 门禁的成本加在"放行"一侧，不加在"怀疑"一侧

如果通过很容易、反对很费力，那门禁在统计上就是不存在的：没人会为了反对去做额外的功课。

**做法**：举证责任放在放行那一方。`approved` 必须列出至少一条自己动手做过的核验，
`changes-requested` 不要求。这次新增的"只剩 minor 的 changes-requested"因为**实际放行了**，
所以照样欠一条自查——它走的是放行那条路，就付放行的价。

出处：`1427315`（checks 的引入）、`04fd933`（放行路径的一致性）。

### 3. 闸门按产物触发，不按发起人触发

"谁发起的"是身份，"产出了什么"是事实。按身份设闸门，会把"你好"送去审核，
同时放过一个悄悄改了文件却什么都不声明的运行——两头都错。

出处：`3661263` —— 审核门从"人类发起的 depth-0 任务"改成"声明了交付物、或动过工作区"。

### 4. 影响判定的状态，必须能从事件日志重建

内存里的计数器和布尔值在重启后会漂移，于是同一份历史在两个进程里得出不同结论。
凡是会决定"要不要叫人"的状态，都要能从 append-only 日志投影出来。

**做法**：与其维护一个 `stallCount++`，不如保存完整历史、用纯函数算出停滞轮数——
这样重启后的结论必然和当时一致，而且这个纯函数能被单独测试和质疑。

**验证方式**：删掉 hydration 里的那几行，测试必须变红。写完这类代码就去试一次；
一个不会因为它失效而失败的测试，并没有在保护它。

出处：球权事件、审核轮次、`04fd933` 的 objection history。

### 5. 旧日志按它写下时的语义回放，不按今天的语义

给一个字段加了新维度之后，最容易犯的错是让旧数据"继承默认值"——而默认值往往是新维度里
最宽松的那一档，等于追认了一批当初根本没被批准的东西。

**做法**：问"这条记录写下的那天，它实际意味着什么"，就按那个回放。
没有严重度的旧 finding 在当时是会卡住任务的，所以回放成 `major`（阻塞），
不能因为"没标严重度"就降级成建议。同理，缺 `reviewType` 的旧审核回放成 `verify`，
因为那时候只有这一种审核。

出处：`3661263`（reviewType）、`04fd933`（bare-string findings）。

### 6. 降级要留痕，而且要告诉被降级的当事人

一个不知道自己被削弱的审核者，没有办法补偿自己的盲区。

**做法**：降级原因写进事件（`reviewerMatch.degradeReasons`），并且直接出现在审核简报里，
用当事人能据此改变行为的措辞——"你和作者同一模型家族，共享它的盲区，先查你自己也会犯的那类错"，
而不是一个只有日志分析时才看得到的枚举值。

出处：`348196c`。

### 7. 措辞改不动的事情，用机制去改

"请不要为了显得严谨而挑刺"写进提示词，模型下一轮照样挑刺——因为它的默认立场被别处
（"默认未通过""怀疑不需要理由"）设死了，两句话打架时赢的不是后写的那句。

**做法**：把想要的行为变成结构：加一个 `severity` 字段，让"挑刺"和"卡住任务"在机制上分开。
提示词只负责解释这个结构，不负责对抗它。

出处：`04fd933`。

### 8. 回滚要写清为什么

`4572e8d` 回滚了 `b6531c0`，回滚提交里只有自动生成的一行 "This reverts commit ..."。
半年后没人知道那个想法是错的、还是只是时机不对，于是下一个 Agent 很可能原样重做一遍。

**做法**：回滚提交必须写清"哪里不对 / 什么条件下可以再来"。如果是个好想法但时机不对，
在这个文件里留一条，比留在被回滚的 commit 里有用得多。

## 变更日志

新的写在最上面。

### 2026-09-04 · 显示位置是另一层判断，不是投影的副产品

**背景**
用户反馈两件事：流式输出时滚动条拉不上去，总被拽回底部；审核意见一直挂在第一条消息下面，越审越难看到。

**教训**

1. **"这条状态属于谁"和"它该画在哪一行"是两个问题。**
   审核状态挂在被审核的那次交付上（taskRunId），这个归属是对的，改它会让投影失真。
   但渲染直接照着归属走，就把三轮审核结论全堆在任务的第一条 Agent 回复底下——
   归属稳定，而阅读顺序是时间。修的时候差点去动 `applyReviewEvent` 的挂载点，
   那会为了一个显示问题污染事实源；正确的做法是加一层纯函数把显示锚点单独算出来
   （`src/web/review-anchors.ts`），投影一个字不改。
   下次再遇到"这个卡片位置不对"，先问是归属错了还是落点错了，别默认要改前者。

2. **自动跟随必须先问用户的视线在哪。**
   无条件 `scrollIntoView` 在没有输出的时候看不出问题，一旦开始流式就等于每秒把
   滚动条从用户手里抢走几十次。判据是"用户本来是不是贴着底部"（留 64px 余量，
   因为内边距和亚像素误差都在这个量级），不是"有没有新内容"。
   同理，平滑滚动在高频更新下会排队、会和滚轮打架，跟随要用瞬时滚动。

**以后怎么做**
前端出现"位置不对"的抱怨时，改的默认落点是渲染层的锚点计算，不是事件投影；
任何"自动帮用户做的动作"（滚动、跳转、聚焦）都要有一个能说出口的、关于用户当前意图的判据。

### 2026-09-03 · `04fd933` · 用停滞判据取代轮数上限

**背景**
用户反馈：几乎每个任务最后都要人工介入，而且理由清一色是"审核轮数用完"。
本意是"分歧很大才找人"，实际变成"聊够两轮就找人"。

**教训**

1. **把上限当判据用，会让这个信号自我毁灭。**（→ 规矩 1）
   `max-rounds` 一开始看着很合理：谈不拢就找人嘛。但两个"差一轮就能谈拢"的 Agent
   和两个"卡在同一堵墙上"的 Agent，走的是同一扇门。事件日志里全是 `max-rounds`，
   于是"需要人工介入"这个信号什么都不再说明。
   诊断的时候差点去调轮数上限（2 → 4）就收工——那只会把问题推迟一轮，不会解决它。
   真正要换的是**判据本身**：从"还能谈几轮"换成"这一轮谈动了没有"。

2. **一个二值的裁决字段，会把所有强度的意见压成同一档。**
   `changes-requested` 既表示"这不能上线"，也表示"这个变量名不好"，
   于是审核者的每一次挑剔都在消耗协商预算。加 `severity` 之前，
   任何"让审核者别太挑"的提示词努力都是在跟机制对抗（→ 规矩 7）。

3. **有些异议不该在 Agent 之间消化。**
   审核者说"原始任务没说超时后该重试还是失败"——这不是缺陷，是需求缺口。
   再谈十轮也只会得到一个论证更漂亮的猜测。这类要立刻抬给人，
   而且抬上去的应该是**那个问题本身**，不是"我们没谈拢"。
   这一条同时说明：找人不等于失败，早点找对人反而更省。

4. **模糊匹配的阈值，要按"错哪一边代价更大"来定，不是按准确率。**
   判断"是不是同一条异议"用的是措辞重合度，必然会有误判。
   误判成"同一条"→ 提前叫人（正是要消除的毛病）；漏判 → 多走一轮（旧行为，可接受）。
   所以阈值取高（0.6 / 0.85），宁漏勿误。这个取舍写进了代码注释，
   否则下一个人看到"准确率不高"会想当然地把它调低。

**以后怎么做**

- 加任何"最多 N 次"的限制之前，先问：N 用完的时候，**发生了什么事实**？
  如果答不上来，这个限制就不该直接接到用户可见的结论上。
- 给裁决类字段加维度（严重度、置信度、可逆性）之前，先看它现在是不是把不同强度的
  东西压成了一档——通常是。
- 改完这类"什么时候叫人"的逻辑，去数一遍：改之前多少条路径通向人、改之后多少条，
  以及每条路径能不能对人说出一句有信息量的话。

**遗留的差距**（自己记着，不是借口）

- 严重度由审核者自己申报，平台不复核。理论上它可以把 nit 标成 blocking 硬卡，
  目前只靠提示词约束。要机制化就得引入按风险轴选验证深度，没做。
- "同一条异议"是措辞重合度这一条机械近似，不是对 failure mode 的语义判断。
  clowder 那边是 Agent 自己判同型，本项目没有那个判断面。
