# 给 Agent 的项目约定

本地对等多 Agent 工作台。架构、协议层和各项配置见 [README.md](README.md)。

## 每次改完代码，三件事

1. **`pnpm run check` 和 `pnpm test` 必须全绿**，再提交。测试跑的是 `dist/`，
   `pnpm test` 会先编译，所以不要跳过它直接跑 `node --test`。
2. **在 [lessons/](lessons/) 里留下一条记录**：挑对应维度的那一卷（前端 / 后端 /
   事件日志 / 协作审核 / 工程流程，路由表在 [lessons/README.md](lessons/README.md)），
   在它的「变更日志」最上面加一条，写清背景、教训、以后怎么做。
   判断标准只有一条——*下一个 Agent 不知道这条会不会重犯*。
   不会就写一条 `### YYYY-MM-DD · 无教训 · <为什么这次没有>`，不要凑流水账；
   那里记的是判断，不是改动清单。同一类教训出现两次以上，提炼进
   [lessons/rules.md](lessons/rules.md)，并保持那一卷简短。
   **这条有机制兜着**：`pnpm run check` 里的 lessons 闸门会在你漏写时变红，
   CI 在 PR 上跑同一道闸门。别去绕开它，绕开的成本比写那一条高得多。
3. **README 与实现同步**。README 描述的是机制而不是愿望，改了行为就改它，
   包括「和 clowder-ai 的差距」那一段——差距缩小了要说，新增了也要说。

## 写代码时

- 事件日志是唯一事实源。任何会决定"要不要叫人"的状态，都要能从日志投影出来，
  不要放在只活在内存里的计数器里。写完去删掉 hydration 那几行验证测试会红。
- 旧事件日志按它写下时的语义回放。新字段缺省值不能追认旧数据没有获得过的待遇。
- 注释解释**为什么**，尤其是取舍和阈值。"选了 0.6 因为错在这一边更贵"这种信息，
  代码本身说不出来，而下一个人会想当然地改掉它。

在做任何非常规的判断之前，先读一遍 [lessons/rules.md](lessons/rules.md)，
再翻一眼你要改的那一层对应的分卷。
